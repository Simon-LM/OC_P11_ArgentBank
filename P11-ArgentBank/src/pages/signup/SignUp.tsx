/** @format */

// const userSchema = z.object({
//     firstName: z.string().min(2).max(50).regex(/^[a-zA-Z]+$/, "Invalid name"),
//     lastName: z.string().min(2).max(50).regex(/^[a-zA-Z]+$/, "Invalid name"),
//     userName: z.string().min(3).max(50),
//     email: z.string().email(),
//   });

//   const validateUser = (data: any) => {
//     return userSchema.safeParse(data); // returns an object with success and error
//   };
